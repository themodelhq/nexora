{
  "name": "@nexora/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Nexora configuration: chain configs, tokenomics, addresses, feature flags.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
