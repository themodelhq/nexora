{
  "name": "@nexora/types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Nexora TypeScript types used across web, admin, api, config, and blockchain packages.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
