{
  "name": "@nexora/e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Nexora end-to-end tests (Playwright).",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0"
  }
}
